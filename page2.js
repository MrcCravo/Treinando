// Repetindo treinamento

let contador = prompt("Vamos Realizar uma contagem até 100. Digite um número menor que 20");



while (contador < 101) {
    console.log(`Nossa contagem está em ${contador}`);
    contador ++
}